#include <bits/stdc++.h>

using namespace std;

int sumar(vector<int>){
    int n = vector.size()
    for (int i=0;i<vector.size()){

    }
}
int main(){
    int t;
    cin>>t;
    vector<int> numeros(t,0);
    while(t--){

    }

    return 0;
}